import { PageStub } from '@/components/ui/PageStub'

export default function BuilderPage() {
  return (
    <PageStub title="Voxel Builder">
      <p className="max-w-md font-mono text-sm text-muted">
        Block editor — Phase 2.
      </p>
    </PageStub>
  )
}
