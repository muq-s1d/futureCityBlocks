import { PageStub } from '@/components/ui/PageStub'

export default function DashboardPage() {
  return (
    <PageStub title="Dashboard">
      <p className="max-w-md font-mono text-sm text-muted">
        Plot, asset library &amp; settings — later phase.
      </p>
    </PageStub>
  )
}
